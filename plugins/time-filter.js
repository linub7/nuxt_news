// import Vue from 'vue';
// import { distanceInWordsStrict } from 'date-fns/';

// Vue.filter('publishedTimeToNow', time => {
//     return `${distanceInWordsStrict(time)} ago`;
// });


// Vue.filter('commentTimeToNow', timestamp => {
//     const elapsedTime = distanceInWordsStrict(timestamp, {
//         includeSeconds: true
//     });
//     return `${elapsedTime} ago`;
// })